import { AppShell } from '@/components/layout/AppShell'
import { VoiceLine } from '@/components/ui/VoiceLine'

export function DayPage() {
  return (
    <AppShell>
      <VoiceLine>
        Day timeline — coming in{' '}
        <b>PR #4</b>
      </VoiceLine>
    </AppShell>
  )
}
