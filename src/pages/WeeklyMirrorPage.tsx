import { AppShell } from '@/components/layout/AppShell'
import { VoiceLine } from '@/components/ui/VoiceLine'

export function WeeklyMirrorPage() {
  return (
    <AppShell>
      <VoiceLine>
        Weekly Mirror — coming in{' '}
        <b>PR #9</b>
      </VoiceLine>
    </AppShell>
  )
}
