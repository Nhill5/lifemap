import { AppShell } from '@/components/layout/AppShell'
import { VoiceLine } from '@/components/ui/VoiceLine'

export function NowPage() {
  return (
    <AppShell>
      <VoiceLine>
        Now / Next focus — coming in{' '}
        <b>PR #4</b>
      </VoiceLine>
    </AppShell>
  )
}
